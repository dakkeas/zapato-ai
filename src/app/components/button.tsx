import React from 'react';

const Button = ({ label }) => {
    return (
        <div>
            <button
            className=""
            >{label}</button>
        </div>
    );
}

export default Button;